The program should be run by using the command line below

$ sudo bash script.sh

The consumer consumes the buffers produced by producer into an output file.
The producer produces the input text into shared memory.
